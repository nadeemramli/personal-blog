filters:
  and:
    - type == "dosing_schedule"
views:
  - type: table
    name: Dosing schedules
    order:
      - code
      - label
      - interval_days
      - days_per_week
      - days_per_week_min
      - days_per_week_max
      - forecast_method
