---
date: 2026-06-12
draft: true
---
# Blog Viz Poster — 2026-06-12 (2nd of the day — Coming Off & PCT)

- **Source brief:** `concepts/Visualization Concept — Fit - Part 4.5 (Performance Enhancement) - Coming Off and PCT.md` (dated 2026-06-12, modified 10:39 — the most recently produced brief; selected over the already-postered 4.4 which has an existing PNG/SVG pair and a record from earlier today)
- **Article:** Fit pillar / Performance Enhancement track / Part 4.5 — Coming Off & PCT (11th PE concept; CLOSES the 6-part "Protection & toolbox" arc 4.0→4.5)
- **Sub-archetype:** Two-stage re-ignition / restart sequencer with a hard mutual-exclusion interlock — the first and only startup-sequence instrument in the Fit set.

## Visual concept used
Landscape (1920×1540) all-electrical ignition schematic. The HPTA is drawn as three horizontal loop rails (HYPOTHALAMUS · PITUITARY · TESTES) that the cycle switched dark, relit left→right along a restart lane: a greyed-out DORMANT LOOP column at left (with the lone lit "pilot light = intra-cycle hCG maintenance, the easy path"), a CLEARANCE RUNWAY clock with an ester-length dial, STAGE 1 lighting the TESTES rail from an EXTERNAL injected signal (hCG→LH, hMG→FSH) in the borrowed warm-amber accent, the HERO INTERLOCK GATE dead-centre (a keyed two-position drive bus stamped "STOP GONADOTROPINS → THEN START SERMs") with the single red FAULT inset beneath it (both stages energized = accelerator + brake together, red-hatched), STAGE 2 lighting the HYPOTHALAMUS rail and flipping the loop colour to SELF-DRIVEN green, and a PHASE 3 VERIFY meter whose RECOVERED lamp is the only green on the poster. A foundation SUPPORT LAYER rail sits beneath the lane wired to nothing ("the floor, not the engine"); a FERTILITY BRANCH forks off Stage 1 (the one case hCG + hMG run together, scorecard = semen analysis); a quiet amber caveat plate ("recovery is not guaranteed/symmetric"); and a bottom RESTART TIMELINE ribbon shows honest phase durations with the interlock drawn as a zero-width hand-off gate. Four-state colour logic per the brief: external/borrowed amber-orange · self-driven green (rationed to "the loop holds on its own") · warning amber · EXACTLY ONE red (the both-stages interlock breach).

## Outputs (authoritative copies)
- SVG: `visualization/Poster — Fit - Part 4.5 (Performance Enhancement) - Coming Off and PCT.svg`
- PNG: `visualization/Poster — Fit - Part 4.5 (Performance Enhancement) - Coming Off and PCT.png` (3840×3080, ~720 KB)

## Renderer & verification
- **Renderer:** CairoSVG (rsvg-convert and inkscape unavailable in sandbox; Playwright not installed — CairoSVG is the available path). Rendered at 2× viewBox (3840×3080).
- **Verified:** PNG non-empty (~720 KB), correct dimensions, visually inspected full + zoomed crops (interlock hero, red fault). All text legible, no clipping, lower band filled by the timeline ribbon.
- **Deviation:** `⛔`/`⚠` emoji rendered as tofu under CairoSVG (no emoji font); replaced with `✕` / `!` glyphs that render in the mono face. No other deviations from the brief.
- **Path note:** the scheduled-task's hard-coded `local_dbaa9cdd … outputs` path remains unmounted in run sessions; the connected `visualization/` folder + trackers are the source of truth.

## Selection note
4.4 (Electrolyte Management) already had a poster pair + record from earlier 2026-06-12; 4.5 is the newest uncovered brief, so this run covered it — consistent with "finish the active track." This CLOSES the Protection & toolbox arc; next poster up = Part 5.0 — The Operating System (Four Pipelines).
