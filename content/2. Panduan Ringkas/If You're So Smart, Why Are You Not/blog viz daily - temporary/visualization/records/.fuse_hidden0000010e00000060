---
draft: true
date: 2026-06-06
type: blog-viz-poster-record
---
# Blog Viz Poster — 2026-06-06 — Sleep Part 3.1: The Pharmacological Reset

- **Date:** 2026-06-06
- **Source brief:** `concepts/Visualization Concept — Healthy - Sleep Part 3.1 - The Pharmacological Reset.md` (blog folder, dated 2026-06-06, confirmed most-recently-modified concept this morning)
- **Article:** Healthy / Sleep / Part 3.1 — The Pharmacological Reset (5th and FINAL Sleep poster — completes the track)
- **Record suffix note:** two earlier records exist for today (Skin 1.1 and Sleep 3.0), so this carries the concept slug per convention.

## Visual concept used

The locked **Clinical Nocturne** register (ink `#0B0F1A` night ground, warm amber `#E8A23D` = body/foundation, cool violet `#8B7CF0` = brain/pharmacology, off-white etched structure, red `#FF5C5C` rationed to a single object). New sub-archetype, the **capstone of the track's spatial chain**: a **30-day suspension bridge in nocturnal engineering elevation** (1.0 built the building → 2.0 sealed the room → 2.1 flew the approach → 3.0 opened the cabinet → 3.1 builds the temporary bridge and then dismantles it).

Reading left → right as the 30 days passing: a **WRECKED-PATTERN near bank** (eroded cliff, fault tags `4 AM ONSET · MONTHS-LONG SPIRAL · SHIFT-WORK DAMAGE`, with the two-ticket `ENTER ONLY IF` legend) → the **deck = a NIGHT 1 → NIGHT 30 timeline** with three week-waypoint chips (`clock dragged into place` / `bed = sleep conditioning` / `debt cleared · normalised`) → three warm-amber **suspension cables = the habits** (`FIXED WAKE + MORNING LIGHT` · `COLD DARK RESERVED ROOM` · `THE WIND-DOWN RITUAL`) carrying the load by night 30 → the **off-ramp / TAPER** with a small walking figure descending and the last segments drawn ghosted/dismantling behind them → the **FOUNDATION far bank** = bedrock + glowing house emblem (the 1.0 building with the 2.0 room warm inside), strata labelled `2.0 THE ROOM · 2.1 THE PROTOCOL · 3.0 THE CABINET`, with the abutment caption `a reset on a broken foundation relapses the moment you stop.`

The bridge's anchor (and the article's own word) is the **DORA cornerstone** — a violet pylon-footing block (`LEMBOREXANT ’19 · DARIDOREXANT ’22 · SUVOREXANT ’14`) with the four-✓ acceptance stamp (`ARCHITECTURE · DEPENDENCE · BREATHING · DRIVE` all passing), an `OREXIN — the wakefulness pedal` lever switched **OFF** inset, and two honesty chips (`$$$ on-patent` / `EMPTY STOMACH · BEFORE BED · ≥7 H`). The lower band's **CHOOSE THE SPAN materials board** carries trazodone (with the dose-inversion ghost-vs-lit bars `DEPRESSION 150–300 mg+` vs `SLEEP 25–50 mg`, `no dopamine, drive intact`), doxepin (the maintenance pick), a dimmed mirtazapine (`WRONG IF CUTTING`), and the agomelatine/ramelteon circadian accelerators (`⚠ liver monitoring`).

The poster's **single red object** is the **false crossing** in the chasm below the deck — a collapsed Z-drug/benzo crutch-bridge that launches from the near bank and loops back / falls short, never touching the foundation (`tolerance ≈ 2 wks → rebound WORSE`, `sedation not architecture`, `relaxes airway → apnoea ↑ → RHR climbs back`, boxed-warning + MY-controlled-medicine chips), keeping its honest slate (not red) concession plate `a few nights' supervised rescue. never the bridge.` Footer = the six-checkpoint **RUNNING THE RESET** inspection rail (EARN IT → DOCTOR + SLEEP LOG → NIGHTLY × 2–4 WKS → TRACK → TAPER & HAND OFF → RELAPSE? · CBT-I = durable fix), the verdict lockup `THE RESET SUCCEEDS WHEN YOU NO LONGER NEED IT.`, the completed series strip `ARCHITECTURE → ROOM → PROTOCOL → CABINET → BRIDGE` (3.1 lit), the second-channel key (`amber holds you up · violet is borrowed · red never reaches the other side`), and the mandatory Rx/controlled-medicine disclaimer. Per §7: no pill-bottle-and-moon hero, no drug-scare theatre (the false crossing fails structurally, keeps its concession), and not a comparison-table-as-hero — the *shape of the crossing* is the argument.

## Outputs

- **SVG:** `visualization/Poster — Healthy - Sleep Part 3.1 - The Pharmacological Reset.svg`
- **PNG:** `visualization/Poster — Healthy - Sleep Part 3.1 - The Pharmacological Reset.png`

## Render / verification

- Renderer: **CairoSVG** (rsvg-convert unavailable in sandbox, per standing note).
- Output: **2400 × 3000** (2× the 1200×1500 portrait viewBox), **~549 KB**, non-empty.
- Read back twice and inspected (full view + hero crop). First pass had a near-bank label / `NIGHT 1` collision → bank fault-tags nudged up and `SHIFT-WORK DAMAGE` restored; a heredoc/edit truncation of the final `</text></svg>` was caught on the second render and repaired. Final render clean.
- No prior poster existed for this concept → no deletes required (only Sleep 1.0/2.0/2.1/3.0 posters were present).

## Deviations / fallbacks

- None of substance. Brief followed faithfully (portrait 1200×1500, hero bridge non-negotiable, single-red discipline preserved). The Write tool could not reach the session `outputs/` scratch path, so the authoritative SVG was authored directly into `visualization/` — same end state as convention requires.
- Path-reconciliation policy from 2026-05-28 still applies: the scheduled-task file's hard-coded `local_*` outputs path is unreachable from the run session; this connected-folder workflow is the source of truth.
